mkdir -pv "${XDG_CONFIG_HOME}"
mkdir -pv "${XDG_CACHE_HOME}"
mkdir -pv "${XDG_DATA_HOME}"
mkdir -pv "${XDG_STATE_HOME}"
mkdir -pv "${XDG_BIN_HOME}"

# Application compliance
mkdir -pv "${XDG_CONFIG_HOME}/XDG"
mkdir -pv "${XDG_BIN_HOME}/XDG"
